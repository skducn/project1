# 注意左侧键名是固定的，只能修改右边的值

[pytest]
python_files = test_*.py    # 默认是test开头文件，这里可以修改，如 demo_*.py, 表示只执行名以demo_开头的文件
python_functions = test_*   # 同上
python_classes = test*  # 同上

# 注册标签
markers =
    qc
    beta
    smoke