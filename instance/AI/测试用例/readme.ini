
# 读取.env
from dotenv import load_dotenv
load_dotenv()

# 从环境变量获取API密钥
# qianwen
DASHSCOPE_API_KEY = os.getenv("DASHSCOPE_API_KEY")
if not DASHSCOPE_API_KEY:
    raise ValueError("请在.env文件中设置DASHSCOPE_API_KEY环境变量")




# 提示词
你是资深软件测试工程师，请解析这份需求文档自动生成标准测试用例。
请严格按照下面要求执行，不要额外解释：
1. 先从需求文档中提取：
- 功能模块
- 字段/参数名称
- 数据类型
- 约束规则（必填/非必填、长度、范围、枚举、格式、正则等）
2. 为每个字段生成三类测试用例，必须全覆盖：
【1】正向用例：符合所有规则的正常合法数据
【2】反向用例：
- 空值（必填字段为空）
- 非法字符
- 格式错误
- 越界（长度过长/过短）
- 数据类型错误
【3】边界用例：
- 最小长度/最小值
- 最大长度/最大值
- 枚举边界（第一个、最后一个）
- 临界值
3. 输出格式要求：
- 直接输出标准 Markdown 表格，方便我一键复制到 Excel
- 列固定为：
- 列固定为：["用例ID", "优先级", "模块", "子模块", "前置条件", "用例类型", "用例检查点", "操作步骤", "预期结果", "备注"]
- 用例ID从 UC001 开始自动递增
- 语言：中文
- 不要多余内容，只输出表格