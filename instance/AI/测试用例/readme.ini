
# 读取.env
from dotenv import load_dotenv
load_dotenv()

# 从环境变量获取API密钥
# qianwen
DASHSCOPE_API_KEY = os.getenv("DASHSCOPE_API_KEY")
if not DASHSCOPE_API_KEY:
    raise ValueError("请在.env文件中设置DASHSCOPE_API_KEY环境变量")




# 提示词
你是资深软件测试工程师，基于这份起搏器数据采集运营管理系统的需求文档，生成标准软件测试用例，严格按以下要求执行，不要额外解释：
1. 先从需求文档中提取：
- 功能模块
- 字段/参数名称
- 数据类型
- 约束规则（必填/非必填、长度、范围、枚举、格式、正则等）
2. 为每个字段生成三类测试用例，必须全覆盖：
【1】正向用例：符合所有规则的正常合法数据
【2】反向用例：
- 空值（必填字段为空）
- 非法字符
- 格式错误
- 越界（长度过长/过短）
- 数据类型错误
【3】边界用例：
- 最小长度/最小值
- 最大长度/最大值
- 枚举边界（第一个、最后一个）
- 临界值
3. 输出格式要求：
- 直接输出标准 Markdown 表格，方便我一键复制到 Excel
- 列固定为：["用例ID","优先级","模块","子模块","前置条件","用例类型","用例检查点","操作步骤","预期结果","web实测结果","状态","是否自动化","自动化校验","自动化脚本","自动化后置","完成时间","备注"]
- 用例ID从 UC001 开始自动递增
- web实测结果，状态，自动化脚本，完成时间，这4列的值均为空
- 是否自动化的值默认为空，如果用例类型的值为正向用例且优先级为高，则标注为"是"
- 自动化校验需要识别需求文档中对应的表格和字段，并生成对应的自动化校验代码，格式参考：
[{"k": "select count(*) as qty from sys_user where job_num='{result['job_num']}'", "v": 1}]，
此语句需替换成对应的数据库查询语句，逻辑是读取这个字符串格式，转换成列表，对列表中的字典进行循环判断，k的值等于v的值进行一个逻辑判断，
其中{result['job_num']}格式不变，只需要适配字段，如job_num，默认不填写内容。
- 自动化后置需要判断测试用例的场景做最后的清理工作，如创建用户，最后要删除用户，格式参考：
[["delete from sys_user where user_name='{result['user_name']}' and job_num='{result['job_num']}'"]]，
此语句需替换成对应的数据库语句，逻辑是读取这个字符串格式，转换成列表，列表里每个列表是一个独立的清理sql，对列表中的列表进行循环判断，
其中{result['user_name']}格式不变，只需要适配字段，如user_name，默认不填写内容。
- 语言：中文
- 不要多余内容，只输出表格

====================================================================================================================================================
你是资深软件测试工程师，基于这份起搏器数据采集运营管理系统需求文档，生成**全维度、高粒度**的标准软件测试用例，严格按以下要求执行，不要额外解释：
1. 输出规范：Markdown表格，用例ID从UC001递增，列固定为：用例ID、优先级、模块、子模块、前置条件、用例类型、用例检查点、操作步骤、预期结果、web实测结果、状态、是否自动化、自动化校验、自动化脚本、自动化后置、完成时间、备注；
2. 覆盖要求：
   （1）按**数据库表字段**拆解：为PROMPT_CATEGORY、PROMPT_INFO、PACEMAKER_MODEL、TASK_OCR_AI_CONFIG等7张核心表的**每个可操作字段**单独生成正向、反向、边界用例；
   （2）按**功能场景**拓展：覆盖单操作、组合操作、批量操作、异常场景、联调场景（如大模型接口调用失败/超时、文件上传失败）；
   （3）按**用户操作流程**生成：覆盖单步操作、多步连续操作、操作回退/取消、重复操作、跨模块联动操作；
3. 用例类型细分：正向用例（正常合法）、反向用例（空值/非法字符/格式错/越界/类型错/重复/关联不存在/接口异常）、边界用例（字段极值/枚举极值/时间临界/数据量临界/分页临界）、场景用例（批量/联调/异常恢复）；
4. 数量要求：系统管理-提问语配置模块至少生成80条，数据导入-起搏器数据模块至少生成150条，核心高优先级用例占比不低于60%；
5. 自动化要求：正向+高优先级用例标「是否自动化=是」，按需求生成**精准的数据库查询校验SQL**和**后置清理SQL**，其他列按要求留空。
6. 输出要求：不要多余内容，只输出表格
====================================================================================================================================================

基于这份起搏器数据采集运营管理系统需求文档，生成**功能+非功能全覆盖**的标准软件测试用例，严格按以下要求执行：
1. 输出规范：Markdown表格，用例ID从UC001递增，列固定为：用例ID、优先级、模块、子模块、前置条件、用例类型、用例检查点、操作步骤、预期结果、web实测结果、状态、是否自动化、自动化校验、自动化脚本、自动化后置、完成时间、备注；
2. 核心覆盖维度（每个维度单独生成用例，无重复）：
   （1）**功能测试**：覆盖提问语配置（增删改查/分页/排序）、起搏器数据（任务创建/查看/重试/结果编辑/导出/查询）所有操作，每个操作正向/反向/边界各≥6条；
   （2）**字段测试**：7张核心表的所有可输入/可配置字段，覆盖最大/最小长度、合法/非法值、空值/默认值/特殊值；
   （3）**场景测试**：批量新增提问语、批量重试失败任务、大文件PDF识别（单文件≥100页）、多文件批量识别（≥50个PDF）、跨日期查询、编辑后导出、接口调用失败重试、文件夹重名/特殊字符文件夹；
   （4）**非功能测试**：页面响应时间（操作后≤2s）、数据兼容性（不同版本PDF/不同格式JSON）、容错性（接口超时/文件损坏/网络中断）、易用性（操作提示/弹窗位置）；
   （5）**联调测试**：大模型File API上传失败、多模态理解接口返回异常、数据库关联查询失败、表外键约束校验；
3. 优先级划分：核心业务操作（新增提问语/创建识别任务/结果导出）为高，辅助操作（分页/排序/单条查询）为中，边界/异常场景为中/低；
4. 数量要求：总用例数≥300条，其中功能用例≥200条，非功能/联调用例≥100条；
5. 自动化要求：高优先级正向用例「是否自动化=是」，生成对应自动化校验（数据库SQL）和后置清理SQL，其他列按要求留空。

----------------------------------
# 提示词
你是资深软件测试工程师，基于这份起搏器数据采集运营管理系统的需求文档，生成**全维度、高粒度**的标准软件测试用例，严格按以下要求执行，不要额外解释：
1. 先从需求文档中提取：功能模块，字段/参数名称，数据类型，约束规则（必填/非必填、长度、范围、枚举、格式、正则等）
2. 用例类型细分：必须全覆盖，正向用例（正常合法）、反向用例（空值/非法字符/格式错/越界/类型错/重复/关联不存在/接口异常）、边界用例（字段极值/枚举极值/时间临界/数据量临界/分页临界）、场景用例（批量/联调/异常恢复）；
3. 用例覆盖要求：
   （1）按**数据库表字段**拆解：为PROMPT_CATEGORY、PROMPT_INFO、PACEMAKER_MODEL、TASK_OCR_AI_CONFIG等7张核心表的**每个可操作字段**单独生成正向、反向、边界用例；
   （2）按**功能场景**拓展：覆盖单操作、组合操作、批量操作、异常场景、联调场景（如大模型接口调用失败/超时、文件上传失败）；
   （3）按**用户操作流程**生成：覆盖单步操作、多步连续操作、操作回退/取消、重复操作、跨模块联动操作；
4. 输出规范：直接输出标准Markdown表格，方便我一键复制到 Excel，用例ID从UC001递增，列固定为：用例ID、优先级、模块、子模块、前置条件、用例类型、用例检查点、操作步骤、预期结果、web实测结果、状态、是否自动化、自动化校验、自动化脚本、自动化后置、完成时间、备注；
   （1）web实测结果，状态，自动化脚本，完成时间，这4列的值均为空
   （2）高优先级正向用例「是否自动化=是」，生成对应自动化校验（数据库SQL）和后置清理SQL，其他列按要求留空。
   （3）自动化校验需要识别需求文档中对应的表格和字段，并生成对应的自动化校验代码，格式参考：[{"k": "select count(*) as qty from sys_user where job_num='{result['job_num']}'", "v": 1}]，此语句需替换成对应的数据库查询语句，逻辑是读取这个字符串格式，转换成列表，对列表中的字典进行循环判断，k的值等于v的值进行一个逻辑判断，其中{result['job_num']}格式不变，只需要适配字段，如job_num，默认不填写内容。
   （3）自动化后置需要判断测试用例的场景做最后的清理工作，如创建用户，最后要删除用户，格式参考：[["delete from sys_user where user_name='{result['user_name']}' and job_num='{result['job_num']}'"]]，此语句需替换成对应的数据库语句，逻辑是读取这个字符串格式，转换成列表，列表里每个列表是一个独立的清理sql，对列表中的列表进行循环判断，其中{result['user_name']}格式不变，只需要适配字段，如user_name，默认不填写内容。
5. 不要多余内容，只输出表格

=========================================================
你是一名资深产品经理，请优化此需求文档结构与功能描述，使其格式规范与逻辑清晰，要求识别文档中的图片，生成需求内容。