每个患者信息4条标签，即3万*4 = 12万
每条就诊记录2条标签，即15万*2 = 30万

请编写一个存储过程，用于生成标签记录（标签表结构参考Cdrd_PO.insert_cdrdPatientTag() 中patient_tag表结构），字段取值的要求是category_source_id对应cdrd_patient_info表中patient_id字段或cdrd_patient_visit_info表中patient_id字段，要求cdrd_patient_info表中每个患者信息生成4条标签，category_key字段值为cdrd_patient_info， category_id值取cdrd_patient_info表中patient_id，tag_id和tag_key值通过与sys_tag_type中category_key关联取tag_id和tag_key，tag_data_id和tag_data_key取sys_tag_data表中tag_data_id和tag_data_key，要求cdrd_patient_visit_info就诊信息表每条就诊记录生成2条标签，category_key值为cdrd_patient_visit_info， category_id值取就诊信息表中patient_visit_id，tag_id和tag_key值通过与sys_tag_type中category_key关联取tag_id和tag_key,tag_data_id和tag_data_key取sys_tag_data表中tag_data_id和tag_data_key，请提高效率，输出完整的代码
